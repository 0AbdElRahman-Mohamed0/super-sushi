/// Each category data model
class CategoryModel {
  /// Name of the category
  String title;

  /// Image path of the category
  String imagePath;

  /// Constructor to implement data
  CategoryModel({required this.imagePath, required this.title});
}
